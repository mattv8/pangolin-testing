# Test artifacts
cookies.txt
results/*.json
results/*.log

# Compiled binaries
bin/

# Backups
*.bak

# Secrets / credentials (never commit)
*.env.local
harbor-creds.txt

# TLS certificates
config/certificates/**/cert.pem
config/certificates/**/key.pem
config/certificates/**/.expires_at
config/certificates/**/.last_update
config/certificates/**/.wildcard
